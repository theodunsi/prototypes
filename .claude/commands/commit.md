---
description: Stage, commit and push changes
---
Review all changes with `git diff`, write a clear commit message, then:
1. `git add -A`
2. `git commit -m "<message>"`
3. `git push` (if remote exists)
